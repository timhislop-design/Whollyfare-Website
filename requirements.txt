# WhollyFare — root requirements for Streamlit Community Cloud deployment
# Streamlit Cloud reads this file automatically from the repo root.

# Core UI
streamlit>=1.35.0

# Data
pandas>=2.0.0

# PDF parsing (Food Lion flyer ingestion)
pdfplumber>=0.10.0
pdf2image>=1.17.0
pytesseract>=0.3.10

# HTTP (Kroger API, USDA FDC)
requests>=2.31.0

# Data validation (engine schemas)
pydantic>=2.0.0

# Document generation (Blueprint)
python-docx>=1.1.0
openpyxl>=3.1.0

# Database — cache-bust 2026-05-19
supabase>=2.0.0,<3.0.0

# Mapping (Grocer Hub store wizard)
folium>=0.17.0
streamlit-folium>=0.20.0

# Browser session persistence (localStorage token store for auth across refreshes)
# POC: streamlit-javascript provides a simple JS eval bridge to localStorage.
# PROD: replace with a custom Streamlit component using HttpOnly cookies.
streamlit-javascript>=0.1.5

# Utilities
python-dateutil>=2.8.0

# Environment / secrets loading
python-dotenv>=1.0.0

# Claude API (PDF circular extraction)
anthropic>=0.25.0

# PDF rendering — pure Python, no system poppler needed (replaces pdftoppm)
pymupdf>=1.23.0
